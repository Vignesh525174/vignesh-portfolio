Place the MathWorks logo image here as `mathworks.png`.

Recommended: use a 200–600px wide PNG or SVG named `mathworks.png`.

Paths used by the site:
- HTML: assets/mathworks.png
- CSS: .skill-logo width is currently 48px; edit `style.css` to change size.

How to add the image:
1. Save the attached logo image to this folder as `mathworks.png`.
2. Refresh the page to see the updated logo.

If you want, reply "I uploaded it" and I will preview and adjust sizing automatically, or reply with a public image URL and I'll switch to that URL.